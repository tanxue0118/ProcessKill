# oom调节（建议800，调得越低压制越激进）
oom_threshold=800
# 深度压制：1=杀所有符合条件的进程，0=只杀子进程（推荐）
deep_press=0
# 轮询间隔（秒，最低30）
poll_interval=30
# 日志最大行数
log_max_lines=100
# 是否记录每个被杀进程明细：1=开，0=关（建议关，省IO）
verbose_kill_log=0
# 是否保护前台应用：1=保护，0=不保护（关闭更省一点，但可能误杀前台）
protect_foreground=1
# 是否启用通知：1=开，0=关
通知=1

